<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AlertDialogActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Kaydet"
        android:id="@+id/buttonKaydet"
        android:layout_above="@+id/buttonUrunSec"
        android:layout_alignLeft="@+id/buttonUrunSec"
        android:layout_alignStart="@+id/buttonUrunSec" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Urun Sec"
        android:id="@+id/buttonUrunSec"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Urun Sec Check"
        android:id="@+id/buttonUrunSecCheck"
        android:layout_below="@+id/buttonUrunSec"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Progress Dialog"
        android:id="@+id/buttonProgress"
        android:layout_below="@+id/buttonUrunSecCheck"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
